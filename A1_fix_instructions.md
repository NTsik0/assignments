The main remaining issue is benchmark clarity, not missing core functionality.

Action items:

- Simplify `assignment-1/sorting-benchmark/src/main/java/benchmark/SortingBenchmark.java` so Bubble Sort has one clearly documented reduced-size path instead of both full-size and reduced-size variants.
- Keep the scalable algorithms at `1,000,000` elements and make sure the report matches the final benchmark structure.

Recommended order:

1. Remove the ambiguous duplicate Bubble Sort scope.
2. Re-run the benchmark.
3. Update the report if needed.
